#ifndef BINARY_TREE_H
#define BINARY_TREE_H

#include "functors.h"
#include "arithmetic_definition.h"
#include "error_handling.h"

static_assert(sizeof(Real) == 8, "The size of 'Real' must be 8 bytes.");

template <typename TPayload, typename TGetVector = Identity>
class KdTree final
{
public:



};

#endif // BINARY_TREE_H
